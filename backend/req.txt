API эндпоинты

🔐 Аутентификация
 • POST /auth/register — регистрация (только client).
 • POST /auth/login — вход, возвращает JWT.

👤 Клиент
 • GET /requests/my — список моих заявок.
 • POST /requests — создать новую заявку.
 • GET /requests/{id} — получить заявку.
 • PUT /requests/{id} — редактировать (опционально).
 • DELETE /requests/{id} — удалить (опционально).

🛠 Администратор
 • GET /admin/requests — список всех заявок
 • PUT /admin/requests/{id}/assign — назначить водителя.
 • PUT /admin/requests/{id}/status — изменить статус (в процессе / выполнено).

🚚 Водитель
 • GET /driver/requests — получить заявки, назначенные водителю.
 • PUT /driver/requests/{id}/status — изменить статус (например, выполнено).